#linux #ubuntu #proxmox 

&nbsp;

## Debian/Ubuntu

```bash
sudo apt install qemu-guest-agent

```

* Start and enable the QEMU Guest Agent service
```bash
sudo systemctl start qemu-guest-agent

```

```bash
sudo systemctl enable qemu-guest-agent

```